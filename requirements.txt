feedparser>=6.0
requests>=2.31
pandas>=2.0
scikit-learn>=1.4
joblib>=1.3
pytest>=8.0

# Phase 3 normalization output depends on extractor behavior, so this dependency is
# intentionally pinned instead of floated like the legacy v1 runtime dependencies.
trafilatura==2.2.0
